using System;
using System.Text;

StringBuilder sb = new StringBuilder();

if (sb.Length == 0)
{
    Console.WriteLine("StringBuilder пуст.");
}
else

    Console.WriteLine("StringBuilder не пуст.");
}